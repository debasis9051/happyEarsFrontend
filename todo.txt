Todo:

Service module
1. when using phone, image can be taken using mobile camera app directly
2. File compression using sharp

Current changes: 
1. pagination for all list apis with tanstack react query
2. restructuring invoices collection
3. Restructuring all backend routes to use proper RESTFUL methods - authentication structure chnage
4. remove config structure from audiometry data
5. change "decibal" -> "decibel" in all places
6. why does accessory get saved when there is no data



//////////////////////////////////
### 2.4 Patient Validation Check
- *Description:* Add a validation check while adding a new patient to prevent duplicate entries. Popup warning when adding a new patient with a name existing in database
- *Acceptance Criteria:*  
  - System should check for existing patients ignoring case sensitivity and blank spaces.  
  - If a similar patient exists, show a warning: "Patient already exists."  
  - Popup should allow the user to:  
    - Confirm it is a new patient and proceed with creation, or  
    - Select the existing patient from the list to avoid duplication.  
///////////////////////////////////





